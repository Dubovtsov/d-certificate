json.extract! personal_datum, :id, :snils, :inn, :passport_s, :passport_n, :issued_by, :date_of_issue, :code, :date_of_birth, :place_of_, :birth, :phone_number, :email, :employee_id, :created_at, :updated_at
json.url personal_datum_url(personal_datum, format: :json)
